export default class Game {
  constructor(gameMap, Players, SoundManager) {
    console.log(gameMap);
    console.log(Players);
    console.log(SoundManager);
  }

  start() {
    console.log('Game started');
  }
}
